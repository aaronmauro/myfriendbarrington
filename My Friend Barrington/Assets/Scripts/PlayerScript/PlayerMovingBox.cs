using UnityEngine;

public class PlayerMovingBox : MonoBehaviour
{
/*    // when box hit moving box
    private void OnCollisionStay(Collision collision)
    {
        if (collision.gameObject.CompareTag(GeneralGameTags.Box))
        {
            // move player with box
            collision.gameObject.findPlayer().isPushingBox = true;
        }
    }
    // when exit the box
    private void OnCollisionExit(Collision collision)
    {
        if (collision.gameObject.CompareTag(GeneralGameTags.Box))
        {
            // move player with box
            collision.gameObject.findPlayer().isPushingBox = false;
        }
    }*/
}
